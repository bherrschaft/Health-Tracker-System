public class User { // Declare a class named User
    private String username; // Private member variable to store the username

    public User(String username) { // Constructor to initialize the User object with a username
        this.username = username; // Set the username for the User
    }

    public String getUsername() { // Method to retrieve the username
        return username; // Return the username
    }
}
